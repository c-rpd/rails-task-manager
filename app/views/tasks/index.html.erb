<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= link_to "delete", task_path(task), data: { turbo_method: :delete, turbo_confirm: "Êtes-vous sure? " } %></li>
  <% end %>
</ul>

<%= link_to "New task", tasks_new_path %>
